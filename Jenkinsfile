pipeline {
    agent any

    stages {
        stage('checkout code') {
            steps {
                sh 'echo "checkout code from dev branch"'
            }
            stage('sonar report') {
            steps {
                sh 'echo "checkout code from dev branch"'
            }
            stage('built') {
            steps {
                sh 'echo "checkout code from dev branch"'
            }
            stage('upload to nexus') {
            steps {
                sh 'echo "checkout code from dev branch"'
            }
        }
    }
}
